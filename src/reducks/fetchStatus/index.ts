import fetchStatusSelector from './selectors'
import { loadingStatusAction, setFetchError, resetFetchError } from './slices'

export default {
    fetchStatusSelector,
    loadingStatusAction,
    setFetchError,
    resetFetchError
}
